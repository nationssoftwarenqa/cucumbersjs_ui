~Create a folder in C drive
~Clone project
~Install node_modules with command git install
~Download chrome drive save understand users in a bin folder ie only if needed
~Install xpath ie only if needed
~Run test with command "npm test or npx cucumber-js"
~If you want to see add a report run the command "npx cucumber-js --tags @tstone —format html:report.html"
